---
type: learning
engine: godot 4.1.1
subject: Godot4教程《勇者传说》
tags:
  - godot
created: 2026-06-24
status: ongoing
---
# 攻击框和受击框

## 📌 学习目标
- 实现 Hitbox（攻击框） 和 Hurtbox（受击框）

## 📖 内容笔记
### 完整代码
#### hitbox.gd
```gdscript
class_name Hitbox
extends Area2D

signal hit(hurtbox)

# 实例化对象时调用
func _init() -> void:
	# 当另一个Area2D节点进入检测区域时触发
	area_entered.connect(on_area_entered)

# 当进入了区域
func on_area_entered(hurtbox:Hurtbox) -> void:
	print("[Hit] %s => %s" % [owner.name,hurtbox.owner.name])
	# 发射命中信号，通知攻击方：我打中了目标
	hit.emit(hurtbox) # ← 这是广播给攻击方（和其他监听者）
	# 发射受击信号，通知受击方：你被打中了
	hurtbox.hurt.emit(self) # ← 这是直接通知受击方
```
#### hurtbox.gd
```gdscript
class_name Hurtbox
extends Area2D

signal hurt(hitbox)
```
#### boar.gd
```gdscript
extends Enemy

enum  State {
	IDLE,
	WALK,
	RUN,
}

@onready var wall_checker: RayCast2D = $Graphics/WallChecker
@onready var player_checker: RayCast2D = $Graphics/PlayerChecker
@onready var floor_checker: RayCast2D = $Graphics/FloorChecker
@onready var calm_down_timer: Timer = $CalmDownTimer

# 能否看见（追击）玩家
func can_see_player()->bool:
	if not player_checker.is_colliding():
		return false
	return player_checker.get_collider() is Player

# 每一帧的逻辑
func tick_physics(state: State,delta: float) -> void:
	match state:
		State.IDLE:
			move(0.0, delta)

		State.WALK:
			move(max_speed / 3, delta)

		State.RUN:
			if wall_checker.is_colliding() or not floor_checker.is_colliding():
				@warning_ignore("int_as_enum_without_cast")
				direction *= -1
			move(max_speed, delta)
			if player_checker.is_colliding():
				calm_down_timer.start()

# 获取下一个状态
func get_next_state(state: State) -> State:
	# 野猪碰见玩家则立即冲向玩家
	if can_see_player():
		return State.RUN
	
	match state:
		State.IDLE:
			# 野猪空闲超过2秒则进入走动状态
			if state_machine.state_time > 2:
				return State.WALK
				
		State.WALK:
			# 野猪碰到墙壁或者前面没路的时候进入空闲状态
			if wall_checker.is_colliding() or not floor_checker.is_colliding():
				return State.IDLE
				
		State.RUN:
			# 野猪的冷静计时器到点后，进入走动状态
			if calm_down_timer.is_stopped():
				return State.WALK
	# 保持当前状态不变
	return state

# 状态转换
@warning_ignore("unused_parameter")
func transition_state(from: State,to: State) -> void:
	print("[%s] %s => %s" %[
		Engine.get_physics_frames(),
		State.keys()[from] if from != -1 else "<START>",
		State.keys()[to],
	])
	match to:
		State.IDLE:
			animation_player.play("idle")
			# 如果前方是墙，则野猪转身
			if wall_checker.is_colliding():
				@warning_ignore("int_as_enum_without_cast")
				direction *= -1
				
		State.WALK:
			animation_player.play("walk")
			# 如果前方是悬崖，则野猪转身
			if not floor_checker.is_colliding():
				@warning_ignore("int_as_enum_without_cast")
				direction *= -1
				# 转身后强制更新射线的缓存
				floor_checker.force_raycast_update()
				
		State.RUN:
			animation_player.play("run")

# 处理受伤事件
func _on_hurtbox_hit(hitbox: Hitbox) -> void:
	print("Poch!")

```

## 💡 关键要点
1. 新建脚本继承自Area2D脚本，重命名为Hitbox、Hurtbox，作为后续自定义节点添加到场景
	- 定义类名、信号（发送通知）
	- 定义进入攻击区域的`on_area_entered`函数逻辑
![[Pasted image 20260625150013.png]]
![[Pasted image 20260626134322.png]]
2. 为Player和Enemy添加`Area2D`节点，改名为`Hitbox`和`Hurtbox`[04:14](https://www.bilibili.com/video/BV1Fp4y1c7zK?t=254.432317#t=04:14.43)，并分别添加`CollisionShape2D`节点编辑形状[04:36](https://www.bilibili.com/video/BV1Fp4y1c7zK?t=276.768437#t=04:36.77)
![[Pasted image 20260625150154.png]]
![[Pasted image 20260625215336.png]]
3. 添加设置碰撞层[04:21](https://www.bilibili.com/video/BV1Fp4y1c7zK?t=261.927519#t=04:21.93)
![[Pasted image 20260625215245.png]]
4. 给Player的AnimationPlayer动画轨道K帧（根据动画启用和禁用攻击框）[07:26](https://www.bilibili.com/video/BV1Fp4y1c7zK?t=446.514178#t=07:26.51)
![[Pasted image 20260625212947.png]]
![[Pasted image 20260625212926.png]]
5. 在脚本里处理受伤的事件[11:01](https://www.bilibili.com/video/BV1Fp4y1c7zK?t=661.240034#t=11:01.24)
	- Hitbox节点hurt的连接信号到方法
	![[Pasted image 20260626134253.png]]
	- boar.dg脚本中会自动创建`_on_hitbox_hit`方法
	![[Pasted image 20260626134223.png]]

## 🔗 参考来源

## 📂 相关笔记

### 系列导航

- TODO: 填写系列笔记链接，如 `[[00.基础项目]] → [[01.相机]] → ...`

### 同系列笔记

```dataview
TABLE type, status, created
FROM ""
WHERE file.frontmatter.subject = this.subject AND file.name != this.file.name
SORT created ASC
LIMIT 10
```
