---
type: learning
engine: godot 4.1.1
subject: Godot4教程《勇者传说》
tags:
  - godot
created: 2026-06-24
status: ongoing
---
# 18.存档与读档

## 📌 学习目标
- 实现游戏的存档与读档（JSON 格式）

## 📖 内容笔记

### 1. JSON 数据类型兼容

JSON 只支持 `null`、`bool`、数字、字符串、数组、字典。Godot 特有类型需要手动转换：

| Godot 类型 | JSON 对应的转换方式 |
|-----------|------------------|
| `NodePath` | `get_path_to(node) as String` |
| `Vector2` | `{ "x": pos.x, "y": pos.y }` |
| `Vector2`（还原） | `Vector2(data.x, data.y)` |

---

### 2. Stats 序列化

在 `Stats.gd` 中添加 `to_dict()` / `from_dict()`：

```gdscript
# Stats.gd
func to_dict() -> Dictionary:
	return {
		max_energy = max_energy,
		max_health = max_health,
		health = health,
	}

func from_dict(dict: Dictionary) -> void:
	max_energy = dict.max_energy
	max_health = dict.max_health    # ⚠️ 必须在 health 之前还原
	health = dict.health            # health setter 受 max_health 限制
```

> [!important] `max_health` 必须在 `health` 之前还原
> `health` 的 setter 用 `clampi(v, 0, max_health)` 限值。如果先还原 `health` 而 `max_health` 还是默认值（3），实际血量可能会被截断。

---

### 3. 存档：`save_game()`

```gdscript
# game.gd
const SAVE_PATH := "user://data.sav"

func save_game() -> void:
	var scene = get_tree().current_scene
	var scene_name = scene.scene_file_path.get_basename()
	world_states[scene_name] = scene.to_dict()    # 手动同步当前场景状态

	var data := {
		world_states = world_states,              # 所有场景的敌人生死状态
		stats = player_stats.to_dict(),           # 玩家血量和能量
		scene = scene.scene_file_path,            # 当前场景路径
		player = {
			direction = scene.player.direction,   # 玩家朝向
			position = {                          # Vector2 → 拆成 x, y
				x = scene.player.global_position.x,
				y = scene.player.global_position.y,
			},
		},
	}
	var json := JSON.stringify(data)
	var file := FileAccess.open(SAVE_PATH, FileAccess.WRITE)
	if not file:
		return
	file.store_string(json)
```

**存档文件路径**：`"user://data.sav"`
- `user://` 在所有平台保证可读可写，用于存放用户数据
- 可在编辑器菜单"项目 → 打开用户数据文件夹"中查看

---

### 4. 读档：`load_game()`

```gdscript
# game.gd
func load_game() -> void:
	var file := FileAccess.open(SAVE_PATH, FileAccess.READ)
	if not file:
		return

	var json := file.get_as_text()
	var data := JSON.parse_string(json) as Dictionary

	change_scene(data.scene, {
		direction = data.player.direction,
		position = Vector2(
			data.player.position.x,
			data.player.position.y
		),
		init = func():
			world_states = data.world_states
			player_stats.from_dict(data.stats)
	})
```

> [!tip] `init` 回调：在黑屏中执行数据恢复
> `init` 是一个 `Callable`（匿名函数），在 `change_scene()` 的 fade out 完成之后、`change_scene_to_file()` 执行**之前**调用。这样 `world_states` 和 `player_stats` 的恢复发生在黑屏期间，不会出现先闪血条再切换的问题。

---

### 5. `change_scene()` 重构

第二个参数从 `entry_point: String` 改为 `params: Dictionary`，支持三种调用方式：

```gdscript
# game.gd — change_scene() 核心逻辑
func change_scene(path: String, params := {}) -> void:
	var tree := get_tree()
	tree.paused = true

	# ... fade out tween ...

	var old_name := tree.current_scene.scene_file_path.get_file().get_basename()
	world_states[old_name] = tree.current_scene.to_dict()

	# 黑屏中执行回调（如恢复 world_states 和 player_stats）
	if "init" in params:
		params.init.call()

	tree.change_scene_to_file(path)
	await tree.tree_changed

	# 场景状态恢复
	var new_name := tree.current_scene.scene_file_path.get_file().get_basename()
	if new_name in world_states:
		tree.current_scene.from_dict(world_states[new_name])

	# 两种放置玩家的方式
	if "entry_point" in params:                          # ① 传送门 → 搜索 EntryPoint 分组
		for node in tree.get_nodes_in_group("entry_points"):
			if node.name == params.entry_point:
				tree.current_scene.update_player(node.global_position, node.direction)
				break

	if "position" in params and "direction" in params:   # ② 读档 → 直接设置位置和朝向
		tree.current_scene.update_player(params.position, params.direction)

	tree.paused = false
	# ... fade in tween ...
```

#### 三种调用方式对比

| 调用方式 | 传入 params | 适用场景 |
|---------|-----------|---------|
| 传送门交互 | `{ entry_point = entry_point }` | Teleporter 脚本 |
| 读档 | `{ direction, position, init }` | load_game() |
| 通用切换 | `{}` | 其他场景切换 |

---

### 6. Teleporter.gd 更新

```gdscript
# Teleporter.gd
class_name Teleporter
extends Interactable

@export_file("*.tscn") var path: String
@export var entry_point: String = ""

func interact() -> void:
	super()
	Game.change_scene(path, { entry_point = entry_point })
```

---

### 7. 测试存档（临时调试）

```gdscript
# game.gd
func _unhandled_input(event: InputEvent) -> void:
	if event.is_action_pressed("ui_cancel"):    # ESC 键
		save_game()
```

---

### 8. Godot 4.2+ 兼容注意事项

> 从 4.2 开始，`change_scene_to_file()` 会先把旧场景从场景树摘下来（不立即销毁）。旧场景的 `StatusPanel` 仍然连着信号，修改 `player_stats` 时可能触发 `create_tween()`——但此时节点已脱离场景树，`create_tween()` 返回 `null`。
>
> 解决方法：在 `StatusPanel` 的 `tree_exited` 信号中断开信号连接，或将 `init` 回调放在 `change_scene_to_file()` 之后执行。

## 💡 关键要点
1. **JSON 数据类型转换** [00:03](https://www.bilibili.com/video/BV1qQ4y1c7c3?t=3)
	- `NodePath` → `as String`，`Vector2` → `{ x, y }`
	- `FileAccess.open(path, mode)` 读写文件，`JSON.stringify()` / `JSON.parse_string()`
2. **Stats.to_dict() / from_dict()** [01:06](https://www.bilibili.com/video/BV1qQ4y1c7c3?t=66)
	- ⚠️ `from_dict` 中 **`max_health` 必须在 `health` 之前还原**，否则血量可能被截断
3. **save_game() — 存档** [02:28](https://www.bilibili.com/video/BV1qQ4y1c7c3?t=148)
	- 手动同步当前场景到 `world_states`，然后构造完整存档字典
	- `SAVE_PATH = "user://data.sav"`，用 `FileAccess.WRITE` + `store_string(json)`
	- 保存内容：`world_states` + `stats` + `scene_path` + `player(position, direction)`
4. **load_game() — 读档** [05:15](https://www.bilibili.com/video/BV1qQ4y1c7c3?t=315)
	- `FileAccess.READ` → `get_as_text()` → `JSON.parse_string()`
	- `change_scene()` 传入 `{ direction, position, init: func() }`
	- `init` 回调在 fade out 完成后执行，避免血条先闪再切换
5. **change_scene() 重构** [06:22](https://www.bilibili.com/video/BV1qQ4y1c7c3?t=382)
	- `params: Dictionary` 替代 `entry_point: String`
	- 支持三模式：`entry_point`（传送门）、`position + direction`（读档）、`init`（数据恢复回调）
6. **Teleporter.gd 更新** [08:07](https://www.bilibili.com/video/BV1qQ4y1c7c3?t=487)
	- `Game.change_scene(path, { entry_point = entry_point })` ← 字典格式
7. **Godot 4.2+ 兼容**（片尾彩蛋）
	- `change_scene_to_file()` 后旧场景离开场景树但未销毁 → StatusPanel 信号触发 `create_tween()` 可能返回 null
	- 方案：`tree_exited` 断开信号连接

## 🔗 参考来源
- 视频教程：[如何保存进度｜Godot 4 教程《勇者传说》#18](https://www.bilibili.com/video/BV1qQ4y1c7c3) — UP主：timothyqiu
- 游戏引擎：Godot 4.1.3（实际项目代码已更新）
- 相关脚本：`Globals/game.gd`, `scripts/Stats.gd`, `scripts/Teleporter.gd`

## 📂 相关笔记

### 系列导航

**系列导航：** [[17.保持场景状态]] → [[18.存档与读档]] → [[19.存档点]]

### 同系列笔记

```dataview
TABLE type, status, created
FROM ""
WHERE file.frontmatter.subject = this.subject AND file.name != this.file.name
SORT created ASC
LIMIT 10
```
