---
type: learning
engine: godot 4.1.1
subject: Godot4教程《勇者传说》
tags:
  - godot
created: 2026-06-24
status: ongoing
---
# 09.三段攻击

## 📌 学习目标
- 实现三段攻击

## 📖 内容笔记

### 完整代码
#### player.gd
```gdscript
extends CharacterBody2D


# 状态机枚举
enum State {
	IDEL,
	RUNNING,
	JUMP,
	FALL,
	LANDING,
	WALL_SLIDING,
	Wall_JUMP,
	ATTACK_1,
	ATTACK_2,
	ATTACK_3,
}


@onready var graphics: Node2D = $Graphics
@onready var animation_player: AnimationPlayer = $AnimationPlayer
@onready var coyote_timer: Timer = $CoyoteTimer
@onready var jump_request_timer: Timer = $JumpRequestTimer
@onready var hand_checker: RayCast2D = $Graphics/HandChecker
@onready var foot_checker: RayCast2D = $Graphics/FootChecker
@onready var state_machine: StateMachine = $StateMachine


# 角色在地面的状态列表
const GROUND_STATES := [
	State.IDEL,State.RUNNING,State.LANDING,
	State.ATTACK_1,State.ATTACK_2,State.ATTACK_3,
]
# 移动速度
const RUN_SPEED := 160.0
# 地面移动加速度
const FLOOR_ACCELERATION := RUN_SPEED / 0.2
# 空中转体加速度
const AIR_ACCELERATION := RUN_SPEED / 0.1
# 是负数的原因是因为在2D空间中y轴向上为负
const JUMP_VELOCITY := -320.0
# 蹬墙跳初速度
const WALL_JUMP_VELOCITY := Vector2(380,-320)

# 获取引擎给的重力加速度
var default_gravity := ProjectSettings.get("physics/2d/default_gravity") as float
# 当前是否第一帧
var is_first_tick := false 
# 记录连击有没有发生
var is_combo_requested = false
#能否连击（作用于动画轨道）
@export var can_combo := false

# 使用事件回调函数_unhandled_input()判断是否按下jump按键
func _unhandled_input(event: InputEvent) -> void:
	if event.is_action_pressed("jump"):
		jump_request_timer.start()
	# 短按轻跳，长按大跳
	if event.is_action_released("jump") :
		jump_request_timer.stop()
		if  velocity.y < JUMP_VELOCITY / 2:
			velocity.y = JUMP_VELOCITY / 2
	
	# 如果按下攻击键并且能够连击，则连击事件发生
	if event.is_action_pressed("attack") and can_combo:
		is_combo_requested = true


# 执行对应状态
func tick_physics(state:State,delta: float) -> void:
	match state:
		State.IDEL:
			move(default_gravity,delta)
			
		State.RUNNING:
			move(default_gravity,delta)
			
		State.JUMP:
			move(0.0 if is_first_tick else default_gravity,delta)
			
		State.FALL:
			move(default_gravity,delta)
			
		State.LANDING:
			stand(default_gravity,delta)
			
		State.WALL_SLIDING:
			move(default_gravity / 3,delta)
			graphics.scale.x = get_wall_normal().x
			
		State.Wall_JUMP:
			if state_machine.state_time > 0.1:
				stand(0.0 if is_first_tick else default_gravity,delta)
				graphics.scale.x = get_wall_normal().x
			else:
				move(default_gravity,delta)
				
		State.ATTACK_1,State.ATTACK_2,State.ATTACK_3:
			stand(default_gravity,delta)
			
	is_first_tick = false

# 移动
func move(gravity:float, delta: float) -> void:
	# 获取按键输入
	var direction :=  Input.get_axis("move_left","move_right")
	# 如果在地面则获取地面加速度，如果不在地面，则获取空中转体加速度
	var acceleration := FLOOR_ACCELERATION if is_on_floor() else AIR_ACCELERATION
	# 修改速度向量
	velocity.x = move_toward(velocity.x, direction*RUN_SPEED, acceleration * delta)
	velocity.y += gravity * delta
	
	# 如果在移动，并且是向左移动，那么将角色水平翻转
	if not is_zero_approx(direction):
		graphics.scale.x = -1 if direction < 0 else +1
	
	move_and_slide()


# 站立
func stand(gravity:float, delta: float) -> void:
	# 如果在地面则获取地面加速度，如果不在地面，则获取空中转体加速度
	var acceleration := FLOOR_ACCELERATION if is_on_floor() else AIR_ACCELERATION
	# 修改速度向量
	velocity.x = move_toward(velocity.x, 0.0, acceleration * delta)
	velocity.y += gravity * delta
	
	move_and_slide()

func can_wall_slide() -> bool:
	return is_on_wall() and  hand_checker.is_colliding() and foot_checker.is_colliding()

# 获取下一个状态
func get_next_state(state:State) -> State:
	# 在地板上或在郊狼时间内，表示能起跳
	var can_jump := is_on_floor() or coyote_timer.time_left >0
	# 能起跳且按下jump按键，则表示应当起跳
	var should_jump := can_jump and jump_request_timer.time_left > 0
	if should_jump:
		return State.JUMP
		
	# 如果在地面状态或者脚下没有地板，则执行下落状态
	if state in GROUND_STATES and not is_on_floor():
		return State.FALL
		
	# 获取按键输入
	var direction :=  Input.get_axis("move_left","move_right")
	# 是否站立状态
	var is_still := is_zero_approx(direction) and is_zero_approx(velocity.x)
	match state:
		State.IDEL:
			if Input.is_action_just_pressed("attack"):
				return State.ATTACK_1
			if not is_still:
				return State.RUNNING

		State.RUNNING:
			if Input.is_action_just_pressed("attack"):
				return State.ATTACK_1
			if is_still:
				return State.IDEL

		State.JUMP:
			if velocity.y >= 0:
				return State.FALL

		State.FALL:
			if is_on_floor():
				return State.LANDING if is_still else State.RUNNING
			if can_wall_slide():
				return State.WALL_SLIDING

		State.LANDING:
			if not is_still:
				return State.RUNNING
			if not animation_player.is_playing():
				return State.IDEL

		State.WALL_SLIDING:
			if jump_request_timer.time_left > 0:
				return State.Wall_JUMP
			if is_on_floor():
				return State.IDEL
			if not is_on_wall():
				return State.FALL
				
		State.Wall_JUMP:
			if can_wall_slide() and not is_first_tick:
				return State.WALL_SLIDING
			if velocity.y > 0:
				return State.FALL
				
		State.ATTACK_1:
			if not animation_player.is_playing():
				return State.ATTACK_2 if is_combo_requested else State.IDEL
				
		State.ATTACK_2:
			if not animation_player.is_playing():
				return State.ATTACK_3 if is_combo_requested else State.IDEL
				
		State.ATTACK_3:
			if not animation_player.is_playing():
				return State.IDEL
			
	return state
	


# 状态转换
func transition_state(from: State,to:State)->void:
	# 之前不处于在地面的状态要切换到地面状态
	if from not in GROUND_STATES and to in GROUND_STATES:
		coyote_timer.stop()
		
	match to:
		State.IDEL:
			animation_player.play("idle")
			
		State.RUNNING:
			animation_player.play("running")
			
		State.JUMP:
			animation_player.play("jump")
			velocity.y = JUMP_VELOCITY
			# 关闭郊狼时间，防止空中再次起跳
			coyote_timer.stop()
			# 关闭跳跃预输入
			jump_request_timer.stop()
			
		State.FALL:
			animation_player.play("fall")
			if from in GROUND_STATES:
				coyote_timer.start()
	
		State.LANDING:
			animation_player.play("landing")
		
		State.WALL_SLIDING:
			animation_player.play("wall_sliding")
			
		State.Wall_JUMP:
			animation_player.play("jump")
			velocity = WALL_JUMP_VELOCITY
			# 通过get_wall_normal()确定X轴的速度方向
			velocity.x *= get_wall_normal().x
			# 关闭跳跃预输入
			jump_request_timer.stop()
			
		State.ATTACK_1:
			animation_player.play("attack_1")
			is_combo_requested = false
		
		State.ATTACK_2:
			animation_player.play("attack_2")
			is_combo_requested = false
		
		State.ATTACK_3:
			animation_player.play("attack_3")
			is_combo_requested = false
	is_first_tick = true

```

## 💡 关键要点
1. 添加Player的3个attack动画（步骤参考前期笔记，此处省略）
	- 不需要开启循环 
	- 在player.gd创建bool类型的导出变量can_combo!![[Pasted image 20260625000818.png]]
	- 在动画轨道添加can_combo变量的关键帧
		- 攻击需要一个ture和一个false
		![[Pasted image 20260625001509.png]]
		- 其他动画仅需要一个false
		![[Pasted image 20260625001442.png]]
2. 在状态枚举中添3个攻击状态，并加入到地面状态数组中
![[Pasted image 20260625002026.png]]
3. 添加bool变量is_combo_requested记录连击事件是否发生[04:23](https://www.bilibili.com/video/BV1a14y1r74A?t=263.982417#t=04:23.98)
![[Pasted image 20260625003129.png]]
4. ATTACK_1、ATTACK_2、ATTACK_3的动画实现逻辑[06:26](https://www.bilibili.com/video/BV1a14y1r74A?t=386.441031#t=06:26.44)
![[Pasted image 20260625003940.png]]
5. ATTACK_1、ATTACK_2、ATTACK_3的状态实现[06:50](https://www.bilibili.com/video/BV1a14y1r74A?t=420.18538#t=06:50.19)
![[Pasted image 20260625004159.png]]
6. 优化下落状态判断
![[Pasted image 20260625003648.png]]
7. 添加ATTACK_1、ATTACK_2、ATTACK_3的状态切换逻辑[05:15](https://www.bilibili.com/video/BV1a14y1r74A?t=315.166023#t=05:15.17)
![[Pasted image 20260625004621.png]]


## 🔗 参考来源

## 📂 相关笔记

### 系列导航

- TODO: 填写系列笔记链接，如 `[[00.基础项目]] → [[01.相机]] → ...`

### 同系列笔记

```dataview
TABLE type, status, created
FROM ""
WHERE file.frontmatter.subject = this.subject AND file.name != this.file.name
SORT created ASC
LIMIT 10
```
